# DGM 歸戶對照表：受試者<TAB>人
#
# 同一個人的多次掃描必須整組在同一個 split，否則就是 data leakage。
# 沒列在這裡的 ID，每一顆各自成一人。
#
# ⚠️ 本檔進公開版控，所以只寫判定「依據的種類」，不寫實際的出生日期／身高／體重／
#    精確掃描日。原始的 DICOM 表頭比對留在 FreeSurfer 端的內部文件，不對外。
#
# 判定依據（2026-09-07）：
#   ① FreeSurfer 端讀 DICOM 表頭：出生日期與身高相同，且「年齡與掃描日期的關係」自洽
#   ② VoxelMorph 端在 atlas 空間量 30 個結構的標籤 Dice
#      DGM 全體 1431 對：中位數 0.6563、第 99 百分位 0.7218
#
# D015 / D037   Dice 0.8561（全 1431 對最高，遠在分布之外）
#   出生日期、身高、體重相同。決定性證據是年齡與掃描日期的一致性：
#   兩次掃描相隔約 3 個月且跨過生日，記錄的年齡差 1 歲，方向與時序一致。
#   複製登錄造不出這個 —— 複製會得到「完全相同的年齡」（見下方 D023/D024）。
#   ⚠️ 兩顆的 PatientSex 相反，至少一邊是錯的 —— 這兩顆的性別欄不要用於分析。
D015	P_D015
D037	P_D015
#
# D038 / DGM002  Dice 0.8528（同樣遠在分布之外）
#   出生日期、身高、性別、年齡相同；體重在約 9 個月間的變化幅度合理。
DGM002	P_D038
D038	P_D038
#
# ── 查過但排除的候選 ────────────────────────────────────────────────
# D025 / D050   Dice 0.6403（低於中位數）—— 出生日期+身高相同是巧合。
#               這批的出生日期只記到年月（日固定填 01），撞號本來就容易發生。
# D023 / D024   Dice 0.6316 —— 同一天連續掃描、人口學欄位完全相同，
#               是技師複製上一位受試者的登錄資料，兩顆是不同人。
# D018 / DGM001 Dice 0.6995 —— DICOM 的年齡與掃描日期方向矛盾，不可能是同一人。
# D015 / D036   Dice 0.6792 —— eTIV 幾乎相同純屬巧合；1431 對裡總會撞到，
#               這也是「形態學指標距離」不能拿來判定同一人的例子。
